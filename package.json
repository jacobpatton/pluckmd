{
  "name": "harvest-monorepo",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rm -rf packages/*/dist",
    "lint": "eslint packages/*/src",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.2.1",
    "eslint": "^9.0.0"
  }
}
